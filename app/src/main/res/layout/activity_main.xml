<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/etRokuIp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/roku_ip_hint"
        android:inputType="text"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnConnect"
        app:layout_constraintHorizontalWeight="1"
        />

    <Button
        android:id="@+id/btnConnect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/connect"
        app:layout_constraintTop_toTopOf="@id/etRokuIp"
        app:layout_constraintBottom_toBottomOf="@id/etRokuIp"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chipGroup"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:singleSelection="true"
        app:layout_constraintTop_toBottomOf="@+id/etRokuIp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <com.google.android.material.chip.Chip
            android:id="@+id/chipRoku"
            style="@style/Widget.Material3.Chip.Assist.Elevated"
            android:text="Roku (Wi‑Fi)"
            android:checked="true"/>

        <com.google.android.material.chip.Chip
            android:id="@+id/chipIR"
            style="@style/Widget.Material3.Chip.Assist.Elevated"
            android:text="Generic IR"/>

    </com.google.android.material.chip.ChipGroup>

    <GridLayout
        android:id="@+id/gridRemote"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:rowCount="5"
        android:columnCount="3"
        android:useDefaultMargins="true"
        app:layout_constraintTop_toBottomOf="@+id/chipGroup"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp">

        <!-- Row 1: Power, Home, Back -->
        <Button android:id="@+id/btnPower" android:text="Power"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnHome" android:text="Home"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnBack" android:text="Back"
            android:layout_width="0dp" android:layout_height="wrap_content"/>

        <!-- Row 2: Up -->
        <Space android:layout_width="0dp" android:layout_height="0dp"/>
        <Button android:id="@+id/btnUp" android:text="▲"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Space android:layout_width="0dp" android:layout_height="0dp"/>

        <!-- Row 3: Left, OK, Right -->
        <Button android:id="@+id/btnLeft" android:text="◀"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnOk" android:text="OK"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnRight" android:text="▶"
            android:layout_width="0dp" android:layout_height="wrap_content"/>

        <!-- Row 4: Down -->
        <Space android:layout_width="0dp" android:layout_height="0dp"/>
        <Button android:id="@+id/btnDown" android:text="▼"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Space android:layout_width="0dp" android:layout_height="0dp"/>

        <!-- Row 5: Vol-, Vol+, Chan-, Chan+ -->
        <Button android:id="@+id/btnVolDown" android:text="Vol-"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnVolUp" android:text="Vol+"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnMute" android:text="Mute"
            android:layout_width="0dp" android:layout_height="wrap_content"/>

        <Button android:id="@+id/btnChanDown" android:text="Ch-"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnChanUp" android:text="Ch+"
            android:layout_width="0dp" android:layout_height="wrap_content"/>
        <Button android:id="@+id/btnInfo" android:text="Info"
            android:layout_width="0dp" android:layout_height="wrap_content"/>

    </GridLayout>

</androidx.constraintlayout.widget.ConstraintLayout>